name: Trivy Image Scanning

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
#      - name: Login to Docker Hub
#        uses: docker/login-action@v2
#        with:
#          username: vigneshviky7
#          password: dckr_pat_6g2hc0QF9ExrX7PZDiRQP7gBIzo
      
      - name: docker build
        uses: docker/build-push-action@v3
        with:
          run: docker build -t dockerbogo/docker-nginx001 .
      - name: run as root for image pick   
          run: docker run -u 0
          
#      - name: Docker Push
#        run: docker push my-app:latest
          
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'docker.io/dockerbogo/docker-nginx001'
          format: 'sarif'
          output: 'trivy-dims-scan-report.sarif'

      - name: Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v2
        with:
          sarif_file: 'trivy-dims-scan-report.sarif'
